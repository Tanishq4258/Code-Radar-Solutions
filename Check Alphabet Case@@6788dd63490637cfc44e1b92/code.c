#include <stdio.h>

int main() {
    char x;
    scanf("%d", &x);
    if(x >= 'a' && y <= 'z'){
        printf("Lowercase");
    }
    else{
        printf("Uppercase");
    }
    return 0;
}